export const getCriticalPath = (data) => {
  const returnArray = {
    critico: [],
    importante: [],
    disponible: []
  }

  const prerrequisitos = []

  data.semesters.map((semester) => {
    semester.courses((course) => {
      
    })
  })

  data.semesters.map((semester) => {
    semester.courses((course) => {
      
    })
  })
}